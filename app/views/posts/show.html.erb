<h1><%= @post.title %></h1>

<!-- Accountability blank category -->

<% if @post.category.present? %>

	<%= link_to @post.category.name, @post.category %>

<% end %>

<%= link_to 'change', edit_post_path(@post) %>

<%= link_to 'delete', post_path(@post), method: :delete, data: { confirm: "Are you sure" } %>

<p><%= @post.body %></p>